/** @type {import('next-sitemap').IConfig} */
const config = {
	siteUrl: 'https://musictosheet.com', // Replace with your site's URL
	generateRobotsTxt: true, // Generates a robots.txt file
	changefreq: 'daily',
	priority: 0.7,
  };
  
  module.exports = config;
  